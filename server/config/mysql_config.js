const config = {
    port: 3000,
    database: {
        DATABASE: 'pet',
        USERNAME: 'root',
        PASSWORD: '1',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config;